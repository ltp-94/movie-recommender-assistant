{
  "annotations": { "list": [] },
  "editable": true,
  "fiscalYearStartMonth": 0,
  "graphTooltip": 0,
  "id": null,
  "links": [],
  "liveNow": false,
  "panels": [
    {
      "title": "Total Conversations",
      "type": "stat",
      "gridPos": { "h": 6, "w": 8, "x": 0, "y": 0 },
      "datasource": { "type": "postgres", "uid": "PostgreSQL" },
      "targets": [
        {
          "datasource": { "type": "postgres", "uid": "PostgreSQL" },
          "editorMode": "code",
          "format": "table",
          "rawSql": "SELECT count(*) FROM conversations WHERE $__timeFilter(timestamp);",
          "refId": "A"
        }
      ],
      "options": {
        "colorMode": "value",
        "graphMode": "area",
        "justifyMode": "auto",
        "reduceOptions": { "calcs": ["lastNotNull"], "fields": "", "values": false }
      }
    },
    {
      "title": "User Feedback Status",
      "type": "piechart",
      "gridPos": { "h": 6, "w": 8, "x": 8, "y": 0 },
      "datasource": { "type": "postgres", "uid": "PostgreSQL" },
      "targets": [
        {
          "datasource": { "type": "postgres", "uid": "PostgreSQL" },
          "editorMode": "code",
          "format": "table",
          "rawSql": "SELECT CASE WHEN feedback > 0 THEN 'Positive' ELSE 'Negative' END as label, count(*) FROM feedback WHERE $__timeFilter(timestamp) GROUP BY 1;",
          "refId": "A"
        }
      ],
      "options": { "legend": { "displayMode": "list", "placement": "right" } }
    },
    {
      "title": "Avg Response Time (sec)",
      "type": "gauge",
      "gridPos": { "h": 6, "w": 8, "x": 16, "y": 0 },
      "datasource": { "type": "postgres", "uid": "PostgreSQL" },
      "targets": [
        {
          "datasource": { "type": "postgres", "uid": "PostgreSQL" },
          "editorMode": "code",
          "format": "table",
          "rawSql": "SELECT avg(response_time) FROM conversations WHERE $__timeFilter(timestamp);",
          "refId": "A"
        }
      ]
    },
    {
      "title": "Recent Conversations",
      "type": "table",
      "gridPos": { "h": 10, "w": 24, "x": 0, "y": 6 },
      "datasource": { "type": "postgres", "uid": "PostgreSQL" },
      "targets": [
        {
          "datasource": { "type": "postgres", "uid": "PostgreSQL" },
          "editorMode": "code",
          "format": "table",
          "rawSql": "SELECT timestamp, question, model_used, response_time, total_cost FROM conversations ORDER BY timestamp DESC LIMIT 10;",
          "refId": "A"
        }
      ]
    }
  ],
  "refresh": "5s",
  "schemaVersion": 38,
  "style": "dark",
  "tags": ["movie-app"],
  "time": { "from": "now-6h", "to": "now" },
  "title": "Movie Assistant Monitoring",
  "uid": "movie-assistant"
}